# Connector notes

Fixture file for the asky-e2e-automation GitHub connector spec (tests/connectors/github-connection.spec.ts).

- Imported as a context note through the Prism GitHub connector.
- Content is stable on purpose: the spec asserts the note exists and is agent-readable, not its text.
